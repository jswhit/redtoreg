Cython function to interpolate ECMWF reduced gaussian gridded data to full gaussian grid.

To test, run `python setup.py build_ext --inplace`, then `python test.py` (to cache data locally), then `python test2.py`.
